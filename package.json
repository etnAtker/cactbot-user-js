{
  "name": "cactbot-user-js",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run typecheck && node scripts/build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.12",
    "json5": "^2.2.3",
    "typescript": "^5.1.6"
  },
  "engines": {
    "node": ">=20.10.0"
  }
}
